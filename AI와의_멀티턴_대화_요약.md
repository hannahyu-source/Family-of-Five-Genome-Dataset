# AI와의 멀티턴(Multi-turn) 대화 요약

**주제**: Kaggle `zusmani/mygenome` 및 `Family of Five` 유전체 데이터셋 분석
**대화 형태**: 총 18턴에 걸친 연속 대화 (탐색 → 실습 → 결과물 제작 → 파이프라인 재구축 → 부가 산출물 생성 → 산출물 개정)
**목적**: 하나의 주제를 여러 턴에 걸쳐 점진적으로 구체화·확장해 나가는 실제 AI 협업 사례를 기록하고, 이를 통해 멀티턴 프롬프트 설계 시 참고할 수 있는 패턴을 정리합니다.

---

## 1. 왜 "멀티턴"이 중요한가

한 번의 프롬프트로 완결되는 싱글턴(single-turn) 요청과 달리, 멀티턴 대화는 이전 턴에서 만든 결과·맥락·용어를 다음 턴이 그대로 이어받습니다. 이번 대화가 좋은 사례인 이유는, 사용자가 처음부터 완성된 요구사항을 제시한 것이 아니라 **"설명 요청 → 아이디어 나열 → 실습 지시 → 결과물 개선 요청 → 범위 재정의 → 전체 재구축 → 부가 산출물 → 기존 산출물의 부분 개정"** 순서로 요구가 점차 구체화·정교화되었기 때문입니다. AI는 매 턴마다 이전 턴에서 확정된 파일 경로, 계산 결과, 용어 정의(예: IBS0, 멘델 오류율), 문서 구조(예: 보고서의 장·절 번호)를 다시 설명하지 않고 그대로 재사용했습니다.

---

## 2. 턴별 타임라인

| 턴 | 사용자 요청 (요지) | AI의 대응 |
|---|---|---|
| 1 | `zusmani/mygenome` Kaggle 데이터셋 설명 요청 | 데이터셋 개요(23andMe raw 포맷, 610,544 SNP) 설명 |
| 2 | 암 위험·체중 경향·조상·유전자-형질 매핑·서구 비교·사인·평생 질병·데이터셋 특이점·개인 특성 등 탐색 질문 9개 나열 | 각 질문을 실제로 답할 수 있는지, 어떤 방법론이 필요한지 구분해서 설명 |
| 3 | "rsid 단위로 SNPedia 등 공개 DB와 대조하는 실습" 요청 | 대표 SNP 16종을 선정해 실제 원자료에서 유전형 조회, 1차 문헌과 대조하는 스크립트 실행 |
| 4 | Kaggle API 자격증명 제공 (데이터 직접 다운로드 시도) | 네트워크 프록시가 Kaggle을 차단하고 있음을 진단 → 우회 대신 사용자에게 파일 업로드를 요청 |
| 5 | 유전체 CSV 파일 7종 업로드 | 파일 구조 파악 후 3번 턴의 실습을 실제 업로드 데이터로 재실행 |
| 6 | "시각화할 수 있는 데이터가 있어?" | 염색체별 SNP 분포, 유전형 구성, 가족 히트맵 등 시각화 3종 생성(HTML 대시보드 + PNG) |
| 7 | archive.zip 업로드 시도 | 파일이 정상 전달되지 않음을 확인, 기존 업로드 파일로 계속 진행 |
| 8 | "최종 보고서에 시각화한 데이터 넣어서 다시 작성해줘" | markdown 보고서 작성(이미지 상대경로 삽입) |
| 9 | "최종 보고서에 이미지 넣어줘" | markdown → **이미지가 실제로 내장된 Word(docx)** 보고서로 형식 전환 |
| 10 | 2번 턴의 질문 9개를 다시 제시하며 "Family of Five 데이터셋에 적용되면 어떻게 변형돼야 할까?" | 개인 데이터용 질문을 가족·미성년자 포함 데이터에 맞게 윤리적으로 재구성 |
| 11 | "이 중에서 답을 얻을 수 있는 질문만 골라줘" | 실제로 데이터로 답변 가능한 질문과 불가능한 질문을 구분해 제시 |
| 12 | "친족관계 검증, 트리오 멘델 오류율, 출신지 일치성 검증, 서구 비교 확장을 실제 코드로 돌려서 결과도 보여줘" | IBS0 기반 친족관계 추정, 멘델 오류율 계산, 조상 마커 대조 코드를 실제 실행하고 결과 제시 |
| 13 | "최종보고서 추가해서 다시 만들어줘" | 12번 턴의 결과를 기존 docx 보고서에 새 섹션으로 통합 |
| 14 | 분석질문 → 데이터사전 → 전처리로그 → 시각화 → 인사이트 → 최종보고서 순서를 명시하며 **Family of Five 5명만으로 파이프라인 전체 재구축**을 "단계별로" 요청 | 6단계를 순서대로 실행하며 매 단계 결과를 확인 → 최종 docx 보고서 완성·전달 |
| 15 | "데이터 전처리용 코랩 파일 생성해줘 (ipynb)" | 14번 턴 파이프라인의 데이터사전·전처리·병합 로직을 Colab에서 바로 실행 가능한 노트북으로 재구성, 실제 커널로 실행해 결과 일치를 검증 후 전달 |
| 16 | "AI와의 멀티턴 md.file 작성새줘" | 세션 전체(당시 15턴)의 흐름을 정리한 요약 문서 1차 버전 작성 — 내용이 모호해 먼저 3가지 선택지를 제시해 되물은 뒤 진행 |
| 17 | 기존 "3. 데이터사전" 항목에 컬럼 사전·행x열·중복행·이상치·결측 셀 점검이라는 **하위 체크리스트를 직접 추가**하며 "적용해서 분석해줘"라고 요청 | 추가된 4개 항목을 실제 5개 파일에 대해 코드로 계산(컬럼별 의미/데이터형표, 완전중복행·rsid중복·위치중복 집계, 이상치 0건 확인, 진짜 결측 셀 0건 확인) → 기존 최종보고서의 2장 구조를 2-1~2-5로 재편해 결과를 반영, 재검증 후 전달 |
| 18 | "AI와의 멀티턴 md.file 새로 작성해줘" | 지금 이 문서 — 17번 턴까지 확장된 흐름을 반영해 요약본을 새로 갱신 |

---

## 3. 이 대화에서 나타난 멀티턴 패턴

### 3-1. 맥락 유지 (Context Carryover)
턴 9("이미지 넣어줘")는 턴 8에서 만든 markdown 보고서를 전제로 한 요청입니다. AI는 "어떤 보고서를 말씀하시는 건가요?"라고 되묻지 않고 직전 산출물을 그대로 이어받아 형식만 전환했습니다. 턴 13, 14, 17도 마찬가지로 "최종보고서", "이 부분"이라는 표현만으로 이전에 만든 특정 파일·특정 절(section)을 정확히 지목했습니다.

### 3-2. 점진적 구체화 (Progressive Refinement)
턴 2의 질문은 추상적("암 위험은?")이었지만, 턴 3에서 "rsid 단위 실습"으로, 턴 12에서 "IBS0·멘델 오류율을 실제 코드로"까지 점점 더 구체적인 실행 단위로 좁혀졌습니다. 멀티턴 대화는 이렇게 큰 방향을 먼저 잡고 세부 실행을 나중에 확정하는 방식에 잘 맞습니다.

### 3-3. 범위 재정의 (Scope Pivot)
턴 10~14는 분석 대상을 "개인 1명(Zeeshan)"에서 "가족 5명"으로 전환하면서, 미성년 자녀가 포함된 데이터라는 제약 조건(질병·인지능력 관련 SNP 배제)을 대화 내내 일관되게 유지했습니다. 이는 한 턴에서 설정된 원칙이 이후 모든 턴(17번 턴의 데이터사전 확장에도)에 걸쳐 적용된 사례입니다.

### 3-4. 명확화 없이 합리적 기본값 선택, 필요할 때만 질문
턴 16처럼 요청이 모호할 때는 AI가 바로 만들지 않고 선택지를 제시해 되물었습니다. 반면 턴 6("시각화할 수 있는 데이터가 있어?")이나 턴 17("적용해서 분석해줘")처럼 맥락상 의도가 명확한 경우는 되묻지 않고 즉시 실행했습니다. 멀티턴 대화에서는 매번 되묻기보다, 맥락으로 판단 가능한 것은 바로 진행하고 진짜 모호한 지점만 질문하는 것이 효율적입니다.

### 3-5. 검증 루프 (Iterative Verification)
후반부(턴 14, 15, 17)에서는 결과물을 만들고 끝내는 것이 아니라, PDF 변환 → 이미지 렌더링 → 육안 확인, 또는 노트북을 실제 커널로 실행해 이전 파이프라인과 수치가 일치하는지 재검증하는 과정을 거쳤습니다. 멀티턴 대화가 길어질수록 이런 검증 단계가 누적된 맥락의 정확성을 지켜주는 역할을 합니다.

### 3-6. 형식(포맷) 요청과 내용(콘텐츠) 요청의 분리
턴 8~9는 내용은 그대로 두고 "형식"만 바꾸는 요청(md → docx)이었고, 턴 12는 "내용"을 새로 추가하는 요청이었습니다. 두 종류의 요청을 구분해서 처리하면, 형식 변경 시 이전 콘텐츠 유실을 방지할 수 있습니다.

### 3-7. 산출물의 부분 개정 (Incremental Amendment) — 새로 관찰된 패턴
턴 17은 이전 턴들과 성격이 다릅니다. 완전히 새로운 요청이 아니라, 사용자가 **이미 존재하는 산출물(보고서의 "3. 데이터사전" 절)에 직접 하위 항목을 덧붙이고**, "이 부분의 하위 내용 추가했어. 적용해서 분석해줘"라는 한 문장으로 변경분만 짚어 전달했습니다. AI는 전체를 다시 만들지 않고, ① 새로 추가된 요구사항만 코드로 계산 → ② 해당 절의 하위 구조(2-1~2-5)만 재편 → ③ 나머지 장(전처리로그, 시각화, 인사이트, 참고자료)은 그대로 유지하는 방식으로 대응했습니다. 이는 소프트웨어 개발의 "diff 기반 수정"과 유사한 패턴으로, 대화가 길어질수록 "전체 재작성"보다 "변경분만 반영"이 더 정확하고 효율적임을 보여줍니다. 이 문서(턴 16→18) 자체도 같은 패턴의 사례입니다 — 완전히 새로 쓰기보다, 이전 버전의 구조를 유지한 채 17번 턴 이후의 흐름만 추가했습니다.

---

## 4. 프롬프트 엔지니어링 실습에 대한 시사점

- **첫 프롬프트를 완벽하게 쓰려 하지 말 것**: 이 대화처럼 큰 주제 → 실습 지시 → 결과물 개선 요청 → 범위 재정의 순으로 자연스럽게 풀어가는 편이, 처음부터 모든 조건을 한 프롬프트에 담으려는 것보다 실전에서 더 유연합니다.
- **이전 턴을 지시어로 참조하기**: "최종 보고서에", "이 중에서", "이 부분의 하위 내용" 같은 지시어만으로도 AI가 대화 맥락에서 정확한 대상을 찾아낼 수 있습니다. 매번 파일명이나 이전 내용을 재설명할 필요가 없습니다.
- **제약 조건은 한 번 명시하면 이후 턴에도 유지되어야 함**: "미성년 자녀 데이터이므로 질병 관련 SNP은 제외"와 같은 원칙은 한 번만 언급해도 이후 모든 관련 작업(데이터사전 확장 포함)에 계속 적용되는지 확인하는 것이 중요합니다.
- **모호한 요청은 선택지를 좁혀서 되묻기**: "무엇을 원하시나요?"라는 열린 질문보다 "A) 개념 정리 B) 실습 예시 C) 세션 요약" 같은 구체적 선택지를 주면 다음 턴이 훨씬 빨라집니다.
- **큰 결과물은 "전체 재작성"보다 "부분 개정"으로 다루기**: 보고서나 문서가 이미 존재한다면, 바뀐 부분만 명확히 짚어 요청하는 편이 AI가 나머지 내용을 실수로 훼손하지 않고 정확히 반영하는 데 도움이 됩니다.

---

*이 문서는 2026-08-04에 진행된 유전체 데이터 분석 세션(총 18턴)을 기반으로 작성되었으며, 턴 16의 최초 버전을 턴 17~18의 흐름을 반영해 갱신한 버전입니다.*
